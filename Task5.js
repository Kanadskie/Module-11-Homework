const numSquare = (x, n) => { // 1. Записываем функциональное выражение с помощью стрелочной функции

    let result = Math.pow(x, n); // 2. Объявляем переменную в которой будет храниться результат выполенния метода Math.pow
    
    console.log(result); // 3. Выводим результат в консоль

  }
  
  numSquare(2, 8); // 4. Выполнение функции с переданными аргументами